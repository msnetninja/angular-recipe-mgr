export class Ingredient {
  // This is a shortcut to access and assign the variables in the declaration.
  // It does the same as our Recipe model implementation, but shorter. 
  constructor(public name: string, public amount: number) { }
}